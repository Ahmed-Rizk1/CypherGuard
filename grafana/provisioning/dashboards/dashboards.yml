# Auto-provision Grafana dashboards from local JSON files
apiVersion: 1

providers:
  - name: SecureNet
    orgId: 1
    folder: SecureNet SOC
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
